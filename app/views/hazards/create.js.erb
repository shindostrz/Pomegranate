// google.maps.event.addListener(marker, 'mouseover', (function(marker) {
//   return function() {
//     console.log(content);
//     infowindows.setContent(content['hazard_type']);
//     infowindows.open(map, marker);
//   };
// })(marker));

// function initialize() {
//   placeMarker(@marker, @new_hazard);
// }